# Leave blank to use the local Vite proxy, which targets http://127.0.0.1:8000.
# For a deployed backend, set this to the FastAPI origin without a trailing slash.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
