import { useState } from 'react'
import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { Activity, Bell, Bot, ChartNoAxesCombined, ChevronRight, HeartPulse, History, Menu, Moon, Search, Settings, ShieldAlert, Sparkles, X } from 'lucide-react'
const links = [{ to: '/', label: 'Dashboard', icon: ChartNoAxesCombined }, { to: '/analyze', label: 'Analyze Incident', icon: Sparkles }, { to: '/similar', label: 'Similar Incidents', icon: Activity }, { to: '/incidents', label: 'Analysis History', icon: History }, { to: '/system-health', label: 'System Health', icon: HeartPulse }, { to: '/settings', label: 'Settings', icon: Settings }]
export function AppShell() { const [open, setOpen] = useState(false); const [search, setSearch] = useState(''); const navigate = useNavigate(); const goSearch = (e: React.FormEvent) => { e.preventDefault(); if (search.trim()) navigate(`/incidents?q=${encodeURIComponent(search.trim())}`) }; return <div className="shell"><aside className={`sidebar ${open ? 'is-open' : ''}`}><div className="brand"><span className="brand-mark"><ShieldAlert size={21}/></span><span>RCA Assistant</span><button className="mobile-close" onClick={() => setOpen(false)} aria-label="Close navigation"><X size={20}/></button></div><nav aria-label="Main navigation">{links.map(({to,label,icon: Icon}) => <NavLink key={to} to={to} end={to === '/'} onClick={() => setOpen(false)}><Icon size={18}/><span>{label}</span><ChevronRight className="nav-chevron" size={15}/></NavLink>)}</nav><div className="sidebar-foot"><a href="mailto:support@example.com"><Bot size={18}/>Help & support</a><p>Evidence-grounded RCA<br/>for operations teams</p></div></aside>{open && <button className="sidebar-backdrop" aria-label="Close navigation" onClick={() => setOpen(false)} />}<main className="workspace"><header className="topbar"><button className="icon-button menu-button" aria-label="Open navigation" onClick={() => setOpen(true)}><Menu size={20}/></button><form className="global-search" onSubmit={goSearch}><Search size={17}/><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search saved RCA analyses…" aria-label="Search saved RCA analyses" /></form><div className="top-actions"><button className="icon-button" aria-label="Notifications"><Bell size={18}/></button><button className="icon-button" aria-label="Toggle theme"><Moon size={18}/></button><span className="profile-placeholder" title="Generic profile placeholder">OP</span></div></header><div className="page"><Outlet /></div></main><nav className="mobile-nav">{links.slice(0,4).map(({to,label,icon: Icon}) => <NavLink to={to} key={to} end={to === '/'}><Icon size={19}/><span>{label.replace(' Incident','').replace('Local ','')}</span></NavLink>)}</nav></div> }
